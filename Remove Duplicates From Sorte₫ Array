class Solution:
    def removeDuplicates(self, nums: List[int]) -> int:
        if not nums:
            return 0
        s=0
        for f in range(1,len(nums)):
            if nums[f]!=nums[s]:
                s=s+1
                nums[s],nums[f]=nums[f],nums[s]
        return s+1
